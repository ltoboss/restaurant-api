export declare class HydraSanitizer {
    private SERVICE_NAME_POSITION;
    private SERVICE_INSTANCE_POSITION;
    private TIME_TO_LIVE;
    private namespace;
    private redisClient;
    private keysAsync;
    private delAsync;
    private hdelAsync;
    private hgetallAsync;
    constructor(redisConfig: any);
    getServiceInstances(serviceName?: string): Promise<unknown[]>;
    getServices(serviceName?: string): Promise<unknown[]>;
    getServicePresence(serviceName: string): Promise<any>;
    getNodes(serviceName?: string): Promise<{}>;
    removeService(serviceName: string): Promise<boolean>;
    redisKeyIsServiceInstanceReference(key: string): boolean;
    removeOldNodes(serviceName?: string): Promise<{}>;
    cleanSingleService(serviceName: string): Promise<void>;
    cleanServices(): Promise<void>;
}
export default HydraSanitizer;
