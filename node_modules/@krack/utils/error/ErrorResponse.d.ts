export default class ErrorResponse extends Error {
    statusCode: number;
    message: string;
    constructor(msg: any);
    toJSON(): {
        message: string;
        statusCode: number;
    };
}
