"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const dotenv = __importStar(require("dotenv"));
const yargs_1 = require("yargs");
function loadEnvironment(path = '.env') {
    console.log(`Loading environment from ${path}`);
    const result = dotenv.config({ path });
    if (result.error) {
        console.error(result.error);
        console.error(`Environment file ${path} could no be loaded. Be sure it exists`);
    }
}
function getEnvironmentPath() {
    const environment = (process.env.ENVIRONMENT
        || yargs_1.argv.environment
        || (yargs_1.argv.production ? 'production' : undefined)
        || (yargs_1.argv.staging ? 'staging' : undefined)
        || (yargs_1.argv.developer ? 'developer' : undefined)
        || (yargs_1.argv.local ? 'local' : undefined));
    const path = environment ? `.env.${environment.toLowerCase()}` : '.env';
    return path;
}
function loadEnv(path) {
    loadEnvironment(path || getEnvironmentPath());
}
exports.loadEnv = loadEnv;
exports.default = loadEnv;
//# sourceMappingURL=env-loader.js.map