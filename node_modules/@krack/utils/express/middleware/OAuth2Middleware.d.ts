export declare class OAuth2Middleware {
    private hydraService;
    private authService;
    constructor(hydra: any);
    authenticateRequest: (req: any, res: any, next: any) => Promise<any>;
    private authenticateRequestThroughHydra;
}
export default OAuth2Middleware;
